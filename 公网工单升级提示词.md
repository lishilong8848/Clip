# 给公网工单程序 AI 的升级提示词

请直接修改现有公网工单项目。旧项目是“公网页面中继命令，内网程序执行工单”的架构，现在改为“公网程序自己创建、执行、拍照、确认、回退并生成 Excel”。不再使用 authority lease、authority ID、authority token、fencing token、内网命令长轮询、ACK 或投影接口。

## 一、必须保留的业务功能

- 支持 `polling` 轮巡工单和 `maintenance` 维保工单。
- 一条轮巡通告可有 1–6 个工单，工单严格串行；维保固定为一个工单。
- 操作人链接只能拍照和操作人确认。
- 审核人链接只能拍照、审核确认和回退上一步。
- 同一时刻两个角色只能进入同一个工单；没有任何照片和确认时才允许释放当前选择。
- 每个步骤必须至少拍一张照片，最多 5 张；每组最多 100 张。
- 每步倒计时从该步真正成为当前步骤时开始，倒计时未结束不得确认。
- 两个角色都需要时，操作人先确认，审核人后确认才进入下一步。
- 审核回退上一步不受倒计时限制；回退后清空上一步和原当前步的确认、照片和计时，两端同步到回退后的当前步骤。
- 全部步骤完成后，公网程序生成一个 `.xlsx`；多次轮巡每个工单一个 Sheet，照片嵌入对应步骤行。
- Excel 模板、样式、文件命名、照片方向校正和 20MiB 上限按旧内网工单逻辑实现。
- 所有状态持久化到 SQLite，照片和 Excel 保存到持久化目录；重启后可继续。

## 二、公共约定

- 协议版本固定为 `1`。
- JSON 成功响应使用 `{"ok": true, "data": {...}}`。
- JSON 失败响应使用 `{"ok": false, "error": {"code": "...", "message": "..."}}`。
- 所有修改操作必须幂等，使用 `Idempotency-Key`或明确的业务唯一键。
- 同一工单使用事务和版本号串行更新，不同工单可并行。
- 无需 OAuth、无需账号登录、无需权威管理、无需站点 ID。
- 角色链接是角色权限凭证；持有链接的人即可以对应角色操作。
- 管理操作使用每个工单独立的 Bearer 令牌，服务器只保存 SHA-256，不保存明文。
- 日志严禁记录角色 secret、Cookie、Bearer 令牌、照片内容。

## 三、必须实现的对接 API

### 1. 健康检查

`GET /api/v1/health`

正常时返回 HTTP 200：

```json
{
  "service": "public_polling_work_order",
  "protocol_version": 1,
  "ready": true,
  "database_ready": true,
  "upload_storage_ready": true
}
```

SQLite 不可用、持久化目录不可写或模板不可用时返回 HTTP 503 且 `ready=false`。

### 2. 创建工单

`POST /api/v1/work-orders`

- 该接口不使用全局权威凭据。
- 必须对 `client_order_id` 建唯一索引。
- 同一 `client_order_id` 和相同请求哈希重复请求时，返回同一条工单和同两条角色链接，不得重复创建。
- 同一 `client_order_id` 但请求哈希不同时返回 HTTP 409。
- 对来源 IP 和全局设置合理限流、数量配额和请求体大小限制，避免公开创建接口被滥用。

请求体示例：

```json
{
  "protocol_version": 1,
  "client_order_id": "wo_xxx",
  "management_token_sha256": "64位小写hex",
  "work_type": "polling",
  "title": "EA118-E楼设备轮巡",
  "scope": "E",
  "sop_name": "二次泵轮巡",
  "operator": {"name": "张三"},
  "reviewer": {"name": "李四"},
  "links": {
    "operator": {
      "link_id": "lnk_xxx",
      "secret_sha256": "64位小写hex",
      "assigned_name": "张三"
    },
    "reviewer": {
      "link_id": "lnk_yyy",
      "secret_sha256": "64位小写hex",
      "assigned_name": "李四"
    }
  },
  "runs": [
    {"run_index": 1, "from_unit": "1#", "to_unit": "2#", "other_unit": "3#"}
  ],
  "steps": [
    {
      "step_key": "1:1",
      "global_index": 0,
      "run_index": 1,
      "run_count": 1,
      "run_label": "1#→2#",
      "step_index": 1,
      "step_count": 3,
      "content": "执行操作",
      "operator_required": true,
      "reviewer_required": true,
      "time_limit_seconds": 30
    }
  ]
}
```

后端必须校验：

- `client_order_id` 和 `link_id` 只能是安全的 URL-safe 字符；
- 三个 SHA-256 必须是 64 位小写 hex；
- `work_type` 只能是 `polling` 或 `maintenance`；
- 操作人和审核人名称不得为空；
- 步骤 1–30 条，每步至少一个角色需要确认，倒计时 0–86400 秒；
- 轮巡工单 1–6 个、维保工单恰好 1 个；
- 设备只能在 `1#–3#` 或 `4#–6#` 同组内指向，起点不得等于终点。

成功返回：

```json
{
  "ok": true,
  "data": {
    "public_group_id": "wo_xxx",
    "state": "active",
    "version": 1
  }
}
```

注意：创建请求不传输角色 secret 和管理令牌明文。公网服务不得伪造或返回 secret；发起程序使用固定格式 `/polling-work-order#link=<link_id>.<secret>` 自行生成两条链接。公网服务只需保存 `link_id` 和 `secret_sha256`。

### 3. 查询工单

`GET /api/v1/work-orders/{public_group_id}`

请求头：`Authorization: Bearer <每单管理令牌>`。将 Bearer 令牌做 SHA-256 后与创建时的 `management_token_sha256` 常量时间比较。

执行中返回：

```json
{"ok": true, "data": {"state": "active", "version": 8}}
```

Excel 已生成返回：

```json
{
  "ok": true,
  "data": {
    "state": "artifact_ready",
    "version": 35,
    "artifact": {
      "name": "E楼2026年9月2日-SOP名-1#轮巡至2#-操作人-张三-审核人-李四-操作记录.xlsx",
      "size": 1234567,
      "sha256": "64位小写hex"
    }
  }
}
```

状态只允许 `active`、`artifact_ready`、`completed`、`cancelled`。`completed` 可作为 `artifact_ready` 的兼容终态，但必须同时返回 artifact。

### 4. 下载成品 Excel

`GET /api/v1/work-orders/{public_group_id}/artifact`

- 使用同一 Bearer 管理令牌。
- 只有 `artifact_ready` 或 `completed` 可下载，否则返回 HTTP 409。
- 返回 Excel 原始二进制，`Content-Type` 使用 xlsx MIME，文件不得超过 20MiB。
- 必须与查询接口中的 `size` 和 `sha256` 一致。

### 5. 取消工单

`POST /api/v1/work-orders/{public_group_id}/cancel`

- 使用 Bearer 管理令牌。
- 支持 `{"reason": "..."}`。
- 幂等：重复取消返回成功。
- 立即失效角色链接和会话，删除尚未归档的临时照片；文件暂时删除失败时标记待清理并由定时任务重试。

## 四、角色链接与页面

- 入口为 `GET /polling-work-order`。
- 页面从 URL fragment 读取 `<link_id>.<secret>`，调用交换接口；服务器对 secret 做 SHA-256 后与对应 `link_id` 记录常量时间比较。
- 交换成功设置 `Secure + HttpOnly + SameSite=Strict` Cookie，然后用 `history.replaceState` 清除 fragment。
- 保留工单总览和独立步骤页两个 HTML 页面；手机可用，步骤页只显示上一条、当前条、下一条。
- 按钮状态全部根据服务器状态计算，不信任前端传入的角色、当前步骤、倒计时或确认状态。
- 拍照支持 JPEG、PNG、WEBP：单张最大 8MiB，解码后最大 40MP，任一边最大 12000px，每步最多 5 张。
- 每张照片支持缩略图、放大和单张删除；删除照片也必须执行角色和当前步骤校验。
- 每次命令携带 `expected_version`，版本冲突返回 HTTP 409 和最新状态。
- 保留页面短轮询或长轮询，另一角色操作后页面可自动刷新。

## 五、Excel 生成

- 全部必要确认完成时后台生成，页面不得被生成过程阻塞。
- 同一工单和相同版本只生成一次；重启后恢复 `generating`。
- 每个工单对应一个 Sheet，写入 SOP 名、设备指向、操作人、审核人、完成时间、实际步骤和全部步骤照片。
- 每步多张照片时，第一张放主图区，其余照片依次新增图片单元格，不覆盖操作说明。
- 手机照片要根据 EXIF 纠正方向，等比例缩放，保证最终 Excel 不超过 20MiB。
- 生成使用临时文件并原子替换，保存后再打开回读，校验 Sheet 数、步骤数和图片数。
- 文件名按现有业务规则：
  - 一次轮巡：`X楼X年X月X日-SOP名-X#轮巡至X#-操作人-姓名-审核人-姓名-操作记录.xlsx`；
  - 多次轮巡：`X楼X年X月X日-SOP名-X#X#轮巡至X#X#-操作人-姓名-审核人-姓名-操作记录.xlsx`；
  - 维保：`X楼X年X月X日-SOP名-操作人-姓名-审核人-姓名-操作记录.xlsx`。

## 六、部署与测试

- 使用单个 Uvicorn worker，SQLite 开启 WAL。
- 8787 或反向代理后的 80/443 需允许所有工单用户访问；不再配置 `/api/v1/internal/*` 出口 IP 白名单，因为这批旧内网中继接口应删除或返回 404。
- 如果有 HTTPS 反向代理，照片请求体上限至少 10MiB，读取超时至少 45 秒。
- SQLite、照片目录、Excel 目录和模板必须使用持久化路径。
- 增加至少以下自动测试：
  1. 创建请求响应丢失后用同一 `client_order_id` 重试，不产生重复工单。
  2. 两个不同 `client_order_id` 可同时执行且不串状态。
  3. 管理令牌、操作人链接、审核人链接权限完全隔离。
  4. 倒计时、拍照上限、操作人先确认、审核回退及多工单串行正确。
  5. 所有步骤完成后生成可打开的 Excel，照片、Sheet 和文件哈希正确。
  6. 重复查询和下载不重复生成文件。
  7. 取消后链接立即失效，待处理操作不再执行。
  8. 数据库不可用、目录不可写、模板缺失时健康检查返回 503。

实现完成后，输出新 API 文档、部署命令、数据目录说明、反向代理示例、数据库迁移说明和全部测试结果。不保留“公网只展示、内网执行”的双路逻辑。
