# 机柜上下电交接记录

更新：2026-09-10。承接任务 `01a067c8-4e6b-7442-b657-f1faa245dae0` 的已确认需求。

## 数据口径

- A/B/C 保留原工作表行；D/E 一柜一条。操作组保存在同一条多维记录，不拆成额外记录。
- 页面读取 `bin/data/cabinet_power/buildings/A.sqlite3` 至 `E.sqlite3`；飞书作为云端主存储。没有定时全表同步。
- 用户保存时持久化操作标识，校验云端版本，逐表上传并回读，本地事务提交后才提示成功。上传异常保留原请求，可以继续核验。
- 当前状态、平面图与导出共享计算；失败操作不覆盖成功状态。历史日/月区域冻结，后续事件统计到增量区域，转换不算新增上/下电。
- 2026-09-10 最新确认：所有楼栋（含 C）按最近成功操作的实际完成时间计算当前状态，不按期望时间或原列位置。原图颜色及 `布局资料.power_baseline` 仅留作历史核对，不再覆盖当前状态。无历史为未上电；仅有无法核实的历史或末次同刻成功状态冲突时显示待核实；失败和缺少实际时间的操作不覆盖已有成功状态。
- 原 XLSM 仅用于生成。页面平面图读取分楼栋预处理文件。

## 实际数据

飞书主表 `tblPuXz8ONJQVrDe`，基础目录 `tblb0MaDE09vGQmM`。

| 楼栋 | 多维/本地记录 | 已确认正式电 | 已确认测试电 | 未上电/已下电 | 待核实 |
| --- | ---: | ---: | ---: | ---: | ---: |
| A | 1031 | 965 | 5 | 18 | 0 |
| B | 1088 | 1012 | 10 | 54 | 0 |
| C | 1220 | 659 | 311 | 28 | 0 |
| D | 988 | 553 | 120 | 315 | 0 |
| E | 1272 | 350 | 52 | 870 | 0 |

共 5599 条，包含 B 楼既有示例；该示例包间名称与楼栋不一致，涉及 302/B04，未擅自删除或改写。

C 楼按用户单独授权，补全 494 条原记录的合并单元格及 55 个历史续行。保持 1220 个原记录 ID，不补造实际完成时间。备份、逐条变更及回读结果在 `bin/data/cabinet_power/backups/C-history-20260910-152929.json`。

C 楼原图红色 939、黄色 31、绿色 28，已上电合计 970。用户曾误写为 673，随后明确确认 970；后续又明确授权 C 楼也全部按最近成功的实际完成操作计算。重算为正式电 659、测试电 311、未上电/已下电 28，总上电仍为 970。304 个机柜的颜色与旧图不同，原始历史数据未改写。A/B/D/E 的原图红色只表示已上电，统一按最近成功操作计算后数量不变。

五楼已上电总数：A 970、B 1022、C 970、D 673、E 402。B 包括图内已上电 1012 及两个运营商机房已编号机柜 10。颜色安装备份及回读日志：`bin/data/cabinet_power/backups/map-colors-20260910-162815.json`；原 5599 条台账未增删。

B-216、B-247 运营商机房独立展示，各 10 柜。目前各有 5 个已知编号、5 柜只有原汇总数量。未虚构编号，支持补登目录与记录。

## 维护与验收

- 备份迁移：`python bin/tools/migrate_cabinet_local.py`。回退前先执行 `--rollback-export`，保存最新业务数据和未完成上传；禁止用旧快照直接覆盖新写入。
- 五楼云端只读回查和原模板验证：`python bin/tools/verify_cabinet_live.py`。已通过，结果在 `bin/data/cabinet_power/exports/verified-original-templates/report.json`。
- 源文件复核：`python bin/tools/audit_cabinet_counts.py`；输出 `output/cabinet-audit/source-counts.json`。
- 后端检查：`python -m unittest bin.test_cabinet_power bin.test_cabinet_reliability`，机柜 42 项及独立末次状态规则 1 项通过，包含异步上传、重试、防重复、历史更正、C 楼最新操作统计和导出。工单链接相关两个测试模块共 56 项通过。
- PC 检查：前端目录执行 `node scripts/check-cabinet.mjs`；包含状态明细、平面图定位、五楼模板、分页、草稿、弹窗、保存与站内后退。
- 并发检查：`python bin/tools/measure_cabinet_local.py`，最新口径下隔离数据五进程导出约 9.06 秒（包含请求入队）；五任务运行区间重叠。预热读取 P95 为 99.4-113.5ms，导出中记录读取 P95 为 21.1ms。五个原模板共享公式节点数均保留。
- 仅对应预览服务使用 8790 端口：`python bin/tools/cabinet_dev_server.py --port 8790`。不重启其他服务。

## 2026-09-10 后续调整

- 台账统一标明实际完成时间；历史抽屉并列显示期望与实际时间，并列出最近成功操作。D 楼 201/B03 的飞书回读与本地一致：2026-08-18 17:57:03 上正式电，2026-08-19 20:00:38 转测试电，当前应为测试电。
- 保存接口可选 `?defer=1`：本地校验及持久化请求后返回受理状态，每楼独立上传执行器继续版本校验、上传、回读和本地提交。前端显示真实阶段，可后台继续、查看提交内容、刷新后恢复查询；状态查询短暂失败保留操作标识。旧同步接口兼容，未完成时不显示成功，不增加全表后台同步。
- 编辑器滚动容器与禁用字段集分离，标题及按钮区固定，长标题换行，多行历史和结果列在较窄 PC 窗口中重排。历史抽屉与机柜确认弹窗一并限定高度和滚动范围；样式只作用于机柜页。
- `python bin/tools/audit_cabinet_event_order.py` 只读检查五楼 5599 条、8113 个解析事件，输出 `output/cabinet-audit/event-order.json`。五楼当前状态与最近成功历史差异均为 0；C 有 304 柜与旧图颜色不同，按最新授权以历史为准。缺少实际时间的历史涉及 B 2 柜、C 335 柜，保留原值。五楼均未发现末次实际时间相同而成功状态冲突；原列顺序不作为当前状态依据。`python bin/tools/verify_cabinet_live.py --local` 验证当前已提交数据的五份新导出，保留此前云端回读验证样本。
- 工单公网链接使用设置中同域名的显式端口，覆盖中继注册返回、已有链接读取和发送入口；路径、查询参数、能力凭据片段不变，不跨域改写。已用隔离消息发送测试覆盖 `:8787`，未真实发消息。运行中的完整桌面服务需重新加载代码后生效，不主动重启非预览服务。
- 最新预览进程为 PID 35604、端口 8790；已确认启动及健康检查成功，其他桌面服务未重启。无未完成机柜写入和任务时更新预览。PC 检查覆盖 1024/1280/1440 窗口、40 组多行历史、80 个事件、抽屉/确认弹窗边界、阶段查询临时失败及提交后刷新恢复；截图在 `output/playwright/cabinet`。

## 范围外回归说明

机柜 43 项、工单 56 项、前端类型检查及构建通过。前端资源路由及非事件重启保护的两个定向回归通过。额外运行 `bin.test_lan_template_work_status` 的全量回归未通过，已停止该扩展运行，并独立复现以下两个与本次修改无关的旧测试不兼容，未擅自修改通告业务代码或测试断言：

- `test_action_jobs_recover_only_before_external_side_effects`（`bin/test_lan_template_work_status.py:22192`）：仍期望无工作类型的未发送任务恢复为 `accepted`；已有 `portal_service.py` 的重启保护仅允许事件任务自动恢复，实际为 `failed`。同文件新的 `test_action_jobs_restart_stops_unsent_non_event_job` 已通过。
- `test_legacy_message_batch_defers_personal_message_until_upload`（`bin/test_lan_template_work_status.py:7617`）：测试的 `_FakeService` 缺少已有队列代码所需的 `get_job` 方法，发生 `AttributeError`。
