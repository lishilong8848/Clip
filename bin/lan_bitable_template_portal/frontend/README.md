# ClipFlow LAN Portal Frontend

This is the Vue 3 production workspace for the LAN portal.

The production portal serves `frontend/dist/index.html` directly when the
dist exists. Do not commit `node_modules` or user runtime data.
